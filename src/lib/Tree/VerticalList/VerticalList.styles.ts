import { StyleSheet } from '../types';

const styles: StyleSheet = {
  mainList: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  line: {
    flex: 1,
    borderWidth: 1,
    borderStyle: 'solid',
  },
};

export default styles;
